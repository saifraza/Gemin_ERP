{
  "name": "@modern-erp/database",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "db:seed": "tsx src/seed.ts",
    "db:seed:procurement": "tsx seed-procurement.ts",
    "db:seed:materials": "tsx seed-materials.ts",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.9.0",
    "prisma": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "zod": "^3.22.0"
  }
}